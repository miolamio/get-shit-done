---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.js, tsconfig.json, prisma/schema.prisma, src/lib/db.ts, tailwind.config.ts]
autonomous: true
requirements: [FOUND-01, FOUND-02]

must_haves:
  truths:
    - "Next.js dev server starts without errors on port 3000"
    - "Prisma connects to PostgreSQL and User model is queryable"
    - "Tailwind utility classes render correctly in browser"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Next.js, Prisma, Tailwind dependencies"
    - path: "prisma/schema.prisma"
      provides: "Database schema with User model"
      contains: "model User"
    - path: "src/lib/db.ts"
      provides: "Prisma client singleton for database access"
      exports: ["prisma"]
  key_links:
    - from: "src/lib/db.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient import and instantiation"
      pattern: "new PrismaClient"
---

<objective>
Scaffold Next.js 14 application with Prisma ORM and PostgreSQL database.

Purpose: Establish the project foundation so all subsequent phases have a working dev environment and database layer.
Output: Runnable Next.js app with Prisma connected to PostgreSQL, User model defined, Tailwind CSS configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Next.js project</name>
  <files>package.json, next.config.js, tsconfig.json</files>
  <action>Create Next.js 14 app with TypeScript, App Router, Tailwind CSS. Use `create-next-app` with --typescript --tailwind --app flags. Verify the generated tsconfig.json has strict mode enabled and paths configured.</action>
  <verify>npm run dev starts without errors</verify>
  <done>Project scaffolded with Next.js 14 + TypeScript + Tailwind</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setup Prisma with PostgreSQL</name>
  <files>prisma/schema.prisma, src/lib/db.ts</files>
  <action>Initialize Prisma with `npx prisma init --datasource-provider postgresql`. Create User model with fields: id (uuid, default), email (unique), name (optional), passwordHash, createdAt, updatedAt. Create src/lib/db.ts with PrismaClient singleton pattern (avoid multiple instances in dev).</action>
  <verify>npx prisma generate succeeds, npx prisma db push works</verify>
  <done>Prisma connected to PostgreSQL with User model</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts Next.js on port 3000
- [ ] npx prisma generate completes without errors
- [ ] npx prisma db push applies schema to database
- [ ] Tailwind classes work in app/page.tsx
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dev environment fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
